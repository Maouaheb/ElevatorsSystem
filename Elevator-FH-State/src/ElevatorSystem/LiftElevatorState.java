package ElevatorSystem;

// The elevator can have three states: stop in next floors of the lift otherwise continuing and finally the lift action
public interface LiftElevatorState {
	// the state class and the elevator can have many states: continue in direction , stop in direction, lift normal system or lift system adapted for handicap
	}
